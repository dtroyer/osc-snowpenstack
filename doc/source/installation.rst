============
Installation
============

At the command line::

    $ pip install osc-snowpenstack

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv osc-snowpenstack
    $ pip install osc-snowpenstack
